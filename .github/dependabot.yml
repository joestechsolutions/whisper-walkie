version: 2

updates:
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    target-branch: master
    open-pull-requests-limit: 5
    # Group all non-breaking patch/minor bumps into a single PR to reduce noise.
    groups:
      python-dependencies:
        patterns:
          - "*"
